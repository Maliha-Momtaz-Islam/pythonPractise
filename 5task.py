#def square(x):
#    return x**2

#y=square(5)
#print(y)

f = lambda x:x**2 #blue x is the parameter and x after : is
                 # the operation we are performing with that parameter
print(f(5))

